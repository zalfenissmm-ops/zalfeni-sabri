# بوت سكالبينغ MT5 — أهداف ربح من 1 إلى 3 دولار

بوت تداول يخدم مباشرة على منصة **MetaTrader 5**، يفتح صفقات صغيرة وسريعة على مدار الساعة،
وكل صفقة عندها هدف ربح **محدّد بالدولار** (من 1 إلى 3 دولار افتراضيًا) وليس بالنقاط.

## قبل ما تبدا: كيفاش تتحسب الربحية

هذي أهم فقرة في الملف. البوت **ما يخلقش ربح من العدم** — هو ينفّذ خطة بانضباط.
حسبة الصفقة الوحيدة:

```
سعر الهدف = سعر الدخول ± (الهدف بالدولار ÷ قيمة النقطة الواحدة على حجم صفقتك)
```

المشكلة الحقيقية هي **السبريد والعمولة**، وهما يتخلّصو في كل صفقة:

| الحجم | قيمة النقطة (EURUSD) | مسافة هدف الـ 1$ | السبريد 10 نقاط يمثّل |
|-------|----------------------|-------------------|------------------------|
| 0.01  | 0.10 $               | 100 نقطة          | 10٪ من الهدف — بعيد وبطيء |
| 0.02  | 0.20 $               | 50 نقطة           | **20٪ — متوازن** |
| 0.10  | 1.00 $               | 10 نقاط           | 100٪ — مستحيل تربح |

يعني الحجم الكبير يقرّب الهدف أما يغرّقه في السبريد، والحجم الصغير يبعّد الهدف
أكثر من قدرة السوق في دقائق. **البوت يحسبلك المجال الصحيح**:

```bash
python3 run_bot.py --check --config config.json
```

ويطلعلك تقرير كيف هذا:

```
--- EURUSD ---
  spread            : 10.0 points
  ATR(14)           : 34.0 points
  target            : $1.00 (29-68 points usable)
  configured volume : 0.02 lot
  workable volume   : 0.015 - 0.035 lot
  suggested volume  : 0.02 lot
  verdict           : OK
```

وكي ما يكونش فما حجم يخدم، يقولها بصراحة (`NOT VIABLE`) بدل ما يفتح صفقات خاسرة.

**الخلاصة الصادقة:** نسبة النجاح لازمها تكون فوق نقطة التعادل بعد السبريد والعمولة.
البوت يعطيك الانضباط والسرعة وإدارة المخاطر — أما الربح على المدى الطويل يتقرّر
بجودة الإشارة وظروف السوق. جرّبه على **حساب تجريبي أسبوع كامل على الأقل** قبل الفلوس الحقيقية.

## التشغيل

### 1. وضع المحاكاة (افتراضي، بلا فلوس وبلا MT5)

```bash
python3 run_bot.py --paper --speed 400 --duration 60 --volume 0.02
```

`--speed 400` يضغط الوقت: ثانية حقيقية = 400 ثانية محاكاة، باش تشوف صفقات بسرعة.

### 2. محاكاة على أسعار حقيقية من MT5

```bash
python3 run_bot.py --feed mt5 --config config.json
```

الأسعار والسبريد من الوسيط متاعك، أما التنفيذ محاكاة — أصدق طريقة للاختبار.

### 3. التداول الحيّ

```bash
pip install -r requirements.txt      # ويندوز فقط
export MT5_LOGIN=12345678
export MT5_PASSWORD='...'
export MT5_SERVER='YourBroker-Demo'

python3 run_bot.py --config config.json --live
```

`--live` وحدها اللي تبعث أوامر حقيقية، وتطلب منك تكتب `LIVE` للتأكيد
(`--yes` تتخطّى السؤال للتشغيل التلقائي).

> مكتبة `MetaTrader5` تخدم على **ويندوز فقط**، ولازم تشغّل البوت على نفس الجهاز
> اللي فيه المنصة، مع تفعيل **Algo Trading** في شريط أدوات MT5.

## كيفاش يخدم البوت

### الدخول

الإشارة تتاخذ **على الشموع المغلقة فقط** — الشمعة اللي مازالت تتبنى تتبدّل مع كل تيك،
والدخول عليها هو أسرع طريقة تتفتحلك صفقة على إشارة تختفي بعد ثانية.

1. **الاتجاه**: EMA سريع (8) فوق/تحت EMA بطيء (21).
2. **الزناد** (واحد من الزوز):
   - **ارتداد**: السعر ينزل للـ EMA السريع ويسكّر راجع فوقه.
   - **اختراق**: السعر يسكّر خارج مدى آخر 3 شموع.
3. **فلتر RSI**: ما يشريش في تشبّع شرائي وما يبيعش في تشبّع بيعي.
4. **فلتر السبريد**: يرفض الصفقة كي السبريد يفوت 35٪ من مسافة الهدف.
5. **فلتر التقلّب**: يرفض هدف أبعد من 2 × ATR — ما عادش سكالبينغ.

### الخروج

| الآلية | السلوك |
|--------|--------|
| **جني الأرباح** | محسوب بالدولار بالضبط، موضوع على سيرفر الوسيط |
| **وقف الخسارة** | إجباري دايمًا، محدود بـ `max_loss_per_trade_usd` |
| **وقف زمني** | يسكّر أي صفقة واقفة أكثر من `max_hold_seconds` |
| **نقطة التعادل** | كي يوصل 60٪ من الهدف، الوقف يتحرّك لسعر الدخول |

### الحمايات

- **حد خسارة يومي** — يوقف كل شيء ويستنّى غدوة.
- **هدف ربح يومي** — اختياري، يوقف وأنت رابح.
- **فترة تهدئة** بعد كل خروج، وسقف للصفقات المفتوحة وللصفقات اليومية.
- **نوافذ حظر** حول التدوير اليومي (`23:56-00:06`) وين السبريد يفتح.
- **كشف السوق المسكّر**: كي السعر ما يتحرّكش، البوت يوقف بدل ما يخدم على أسعار ميتة.
- **إعادة اتصال تلقائية** بـ backoff متصاعد كي تطيح المنصة.

### الوقت

كل التوقيتات في الإعدادات وفي حساب اليوم **بتوقيت UTC**. MT5 يرجّع توقيت السيرفر
متاع الوسيط، والبوت يقيس الفرق تلقائيًا من التيكات الحيّة ويحوّل كل شيء لـ UTC،
باش حساب ربح اليوم ونوافذ الحظر يبقاو صحاح مهما كانت المنطقة الزمنية متاع الوسيط.

## الإعدادات

انسخ `config.example.json` وبدّل فيه. أهم المفاتيح:

| المفتاح | المعنى |
|---------|--------|
| `symbols` | قائمة الأزواج، مثلاً `["EURUSD", "GBPUSD"]` |
| `volume` | حجم اللوت — شغّل `--check` باش تعرف المجال الصحيح |
| `target_profit_min_usd` / `target_profit_max_usd` | مجال الهدف بالدولار (1 إلى 3) |
| `atr_target_fraction` | نسبة الـ ATR اللي يستهدفها: سوق نشيط = هدف أكبر |
| `commission_per_lot_usd` | عمولة الذهاب والإياب — تتزاد فوق الهدف باش الصافي يبقى في المجال |
| `max_loss_per_trade_usd` | سقف الخسارة في الصفقة |
| `daily_loss_limit_usd` | سقف الخسارة اليومي — الحماية الأهم |
| `max_hold_seconds` | أقصى مدة للصفقة قبل الوقف الزمني |
| `max_spread_points` | أقصى سبريد مقبول للدخول |
| `magic` | رقم تعريف البوت — يخلّيه يتعامل مع صفقاته هو فقط |

بيانات الدخول تتحطّ في متغيرات البيئة `MT5_LOGIN` و `MT5_PASSWORD` و `MT5_SERVER`،
موش في ملف الإعدادات.

## المخرجات

- **`bot.log`** — كل قرار، بما فيه أسباب رفض الصفقات.
- **`trades.csv`** — سجل بصيغة CSV: الفتح، الغلق، الوقف الزمني، والرفض، مع الربح بالدولار.

```
2026-08-28 11:52:24 INFO OPEN BUY EURUSD 0.02 @ 1.10004 | TP 1.10055 (+$1.00) SL 1.09955 (-$1.00) | 3-bar breakout (RSI 61)
2026-08-28 11:52:30 INFO CLOSED EURUSD #1  +1.02 USD  |  day +1.02 USD over 1 trade(s)
```

## بنية المشروع

```
mt5_bot/
  models.py       أنواع البيانات المشتركة + تحويل النقاط للدولار
  config.py       الإعدادات والتحقق منها
  indicators.py   EMA / RSI / ATR
  strategy.py     منطق الدخول
  sizing.py       تحويل الهدف بالدولار لأسعار TP/SL
  risk.py         حدود اليوم والتهدئة وسقوف الصفقات
  preflight.py    فحص: هل الهدف قابل للتحقيق على هذا الوسيط؟
  broker.py       الواجهة الموحّدة للوسيط
  mt5_broker.py   التنفيذ الحيّ على MT5
  paper_broker.py المحاكاة (مغذي صناعي أو أسعار MT5 حقيقية)
  engine.py       حلقة التداول
  journal.py      سجل CSV
run_bot.py        نقطة التشغيل
```

نفس الاستراتيجية ونفس إدارة المخاطر تخدم في المحاكاة وفي الحيّ — الفرق الوحيد
هو الوسيط اللي يتحقن للمحرّك.

## الاختبارات

```bash
python3 -m unittest discover -s tests -t . -v
```

88 اختبار يغطّيو حساب الهدف بالدولار، فلاتر الدخول، حدود المخاطر، والدورة كاملة
من الإشارة للتنفيذ للسجل.

## تنبيه

التداول بالرافعة المالية فيه مخاطرة خسارة رأس المال. هذا الكود أداة تنفيذ، موش
نصيحة مالية ولا ضمان ربح. ابدا بحساب تجريبي، وما تخاطرش بفلوس تحتاجها.
